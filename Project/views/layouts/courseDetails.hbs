<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Курс: {{course.course_name}}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-image: url('/background.png');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            min-height: 100vh;
            margin: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
        }

        .container {
            background-color: rgba(255, 255, 255, 0.95);
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.3);
            animation: fadeIn 0.8s ease-in-out;
            max-width: 800px;
            width: 90%;
            margin-top: 100px; /* Учитываем фиксированный хедер */
            margin-bottom: 40px;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        h1 {
            color: #333;
            font-size: 28px;
            margin-bottom: 20px;
        }

        .course p {
            font-size: 18px;
            color: #444;
            line-height: 1.6;
            text-align: left;
            margin-bottom: 15px;
        }

        .course strong {
            color: #222;
        }

        .btn {
            display: inline-block;
            background-color: #007bff;
            color: #fff;
            text-decoration: none;
            padding: 12px 24px;
            border-radius: 8px;
            transition: background-color 0.3s ease, transform 0.2s;
            font-size: 16px;
            border: none;
            cursor: pointer;
            margin-top: 20px;
            width: 100%;
            max-width: 300px;
            text-align: center;
        }

        .btn:hover {
            background-color: #0056b3;
            transform: scale(1.05);
        }

        .back-link {
            display: block;
            margin-top: 20px;
            font-size: 16px;
            color: #007bff;
            text-decoration: none;
            transition: color 0.3s;
        }

        .back-link:hover {
            color: #0056b3;
        }
    </style>
</head>

<body>
    {{> header}}

    <div class="container">
        <h1>{{course.course_name}}</h1>

        <div class="course">
            <p><strong>Описание:</strong> {{course.description}}</p>
            <p><strong>Тип курса:</strong> {{type.type_name}}</p>
            <p><strong>Продолжительность:</strong> {{course.duration}} часов</p>

            <button class="btn details-button" data-course-id="{{course.course_id}}">Записаться!</button>
        </div>

        <a href="/courses" class="back-link">← Назад к курсам</a>
    </div>

    <script>
        document.querySelector('.details-button').addEventListener('click', () => {
            const currentUrl = window.location.href;
            const courseId = currentUrl.substring(currentUrl.lastIndexOf('/') + 1);
            const newUrl = `/entry?courseId=${courseId}`;
            window.location.href = newUrl;
        });
    </script>
</body>

</html>
