<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Ваш профиль</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #f0f0f0, #c0c0c0);
            animation: gradientAnimation 10s ease infinite;
        }

        @keyframes gradientAnimation {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        header {
            position: relative;
            z-index: 10; /* Убедитесь, что header выше фоновых элементов */
        }

        .container {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            position: relative;
            z-index: 1;
        }

        h1 {
            font-size: 2rem;
            font-weight: bold;
            margin-bottom: 1rem;
            text-align: center;
            color: #4567b7;
        }

        nav {
            background-color: #4567b7;
            padding: 1rem;
            border-radius: 8px;
            margin-top: 1rem;
        }

        nav ul {
            list-style: none;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
        }

        nav ul li a {
            color: #fff;
            text-decoration: none;
            padding: 0.5rem 1rem;
            transition: background-color 0.3s ease;
        }

        nav ul li a:hover {
            background-color: #334ccc;
        }

        .user-info {
            text-align: center;
            margin-bottom: 20px;
        }

        .user-info p {
            margin: 5px 0;
            font-size: 1.2rem;
            color: #333;
        }

        .background-elements {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 0; /* Фоновые элементы должны быть ниже header */
            overflow: hidden;
        }

        .background-elements div {
            position: absolute;
            width: 20px;
            height: 20px;
            background-color: rgba(69, 103, 183, 0.1);
            border-radius: 50%;
            animation: float 5s infinite ease-in-out;
        }

        @keyframes float {
            0% { transform: translateY(0) rotate(0deg); }
            50% { transform: translateY(-20px) rotate(180deg); }
            100% { transform: translateY(0) rotate(360deg); }
        }
    </style>
</head>
<body>
    {{>header}} <!-- header должен быть выше фоновых элементов -->
    <div class="background-elements">
        <div style="top: 10%; left: 20%;"></div>
        <div style="top: 30%; left: 50%;"></div>
        <div style="top: 70%; left: 80%;"></div>
        <div style="top: 50%; left: 10%;"></div>
    </div>
    <div class="container">
        <h1>Добро пожаловать в ваш профиль, {{user.name}}</h1>
        <div class="user-info">
            <p><strong>Имя пользователя:</strong> {{user}}</p>
            <p><strong>Электронная почта:</strong> {{email}}</p>
        </div>
        {{>profileHeader}}
    </div>
</body>
</html>