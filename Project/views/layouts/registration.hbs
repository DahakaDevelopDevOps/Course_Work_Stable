{{!-- <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Курсы по программированию</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KyZXEAg3QhqLMpG8r+Knujsl7/1L_dstPt3HV5HzF6Gvk/e3s4Wz6iJgD/+ub2oU" crossorigin="anonymous">
  <style>
    body {
      background-color: #f5f5f5;
      font-family: Arial, sans-serif;
    }

    .container {
      width: 500px; /* increased width */
      margin: 0 auto;
      background-color: #fff;
      padding: 40px; /* increased padding */
      border-radius: 10px; /* increased border radius */
      box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.2); /* increased box shadow */
      animation: fadeIn 1s ease; /* fadeIn animation */
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
      }
      to {
        opacity: 1;
      }
    }

    .form-label {
      font-weight: bold;
      margin-bottom: 15px; /* increased margin */
    }

    .form-control {
      border-radius: 8px; /* increased border radius */
      border: 1px solid #ccc;
      padding: 15px; /* increased padding */
      width: 100%;
      box-sizing: border-box;
      margin-bottom: 20px; /* increased margin */
      transition: border-color 0.3s ease; /* transition for border color */
    }

    .form-control:focus {
      border-color: #007bff; /* change border color on focus */
    }

    .btn-primary {
      background-color: #007bff;
      color: #fff;
      border: none;
      padding: 15px 30px; /* increased padding */
      border-radius: 8px; /* increased border radius */
      cursor: pointer;
      transition: background-color 0.3s ease; /* transition for background color */
      margin-right: 10px; /* spacing between buttons */
    }

    .btn-primary:hover {
      background-color: #0062cc;
    }
  </style>
</head>
<body>
  <div class="container mt-5">
    <form id="registration-form" form action="/auth/register" method="POST">
      <div class="mb-4"> <!-- increased margin -->
        <label for="username" class="form-label">Username</label>
        <input type="text" class="form-control" name="username"  id="username" pattern="[a-zA-Z0-9]{4,}" title="Username must contain only letters and numbers, and be at least 4 characters long" required>
      </div>
      <div class="mb-4"> <!-- increased margin -->
        <label for="email" class="form-label">Email address</label>
        <input type="email" class="form-control" id="email" name="email" aria-describedby="emailHelp" required>
        <div id="emailHelp" class="form-text">We'll never share your email with anyone else.</div>
      </div>
      <div class="mb-4"> <!-- increased margin -->
        <label for="password" class="form-label">Password</label>
        <input type="password" class="form-control" id="password" pattern=".{8,}" name="password" title="Password must be at least 8 characters long" required>
      </div>
      <div class="mb-4"> <!-- increased margin -->
        <label for="confirm-password" class="form-label">Confirm Password</label>
        <input type="password" class="form-control" id="confirm-password" pattern=".{8,}" title="Password must be at least 8 characters long" required>
      </div>
      <button type="submit" class="btn btn-primary" onclick="redirectToLoginPage()">Register</button>
      <button type="button" class="btn btn-primary" onclick="redirectToLoginPage()">Back to Login</button>
    </form>
  </div>

  <script>
    function redirectToLoginPage() {
      window.location.href = 'http://localhost:3000/auth/login';
    }
  </script>
</body>
</html> --}}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Регистрация</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f2f2f2;
        }
        a {
            color: #007bff;
            text-decoration: none;
        }
        .container {
            width: 100%;
            max-width: 600px;
            padding: 20px;
            margin: 0 auto;
            background-color: #fff;
            border-radius: 5px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .form-control {
            width: calc(100% - 20px); /* Adjusted width */
            padding: 10px;
            margin-bottom: 15px; /* Added margin bottom */
            border-radius: 5px;
            border: 1px solid #007bff;
        }
        .btn {
            padding: 10px;
            margin-top: 10px; /* Added margin top */
            border-radius: 5px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
        }
        .btn-primary {
            background-color: #007bff;
            color: #fff;
            border: none;
        }
        .btn-primary:hover {
            background-color: #0056b3;
        }
        .btn-secondary {
            background-color: #6c757d;
            color: #fff;
            border: none;
        }
        .btn-secondary:hover {
            background-color: #545b62;
        }
        label {
            margin-bottom: 15px;
            display: block; /* Ensures labels stack vertically */
        }
    </style>
</head>
<body>
    <div>
        {{> back}} 

        <form action="/auth/register" method="POST" class="container">
            <label>
                Username
                <input class="form-control" type="text" name="username" required />
            </label>

            <label>
                Email
                <input class="form-control" type="text" name="email" required />
            </label>

            <label>
                Password
                <input class="form-control" type="password" name="password" required />
            </label>

            <button class="btn btn-primary" type="submit">
                Sign Up
            </button>
            <button class="btn btn-secondary" type="button" onclick="location.href = '/auth/login'">
                Already have an account?
            </button>
        </form>
    </div>
</body>
</html>
